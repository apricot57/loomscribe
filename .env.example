# LoomScribe Environment Configuration
PORT=3000

# Optional: Server-side DeepSeek API key (can also be configured via Settings in the UI)
DEEPSEEK_API_KEY=

# Login password to protect the UI (leave blank to disable auth)
APP_PASSWORD=
